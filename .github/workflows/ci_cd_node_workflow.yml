name: Node.js CI/CD

on:
  push:
    branches:
      - main
        
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Demo Test
        run: echo "This is a demo test step. Replace with actual tests."