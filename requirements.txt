# ─────────────────────────────────────────────────────────────
# NOTE: This is a Node.js project.
# Node.js does not use requirements.txt — dependencies are
# declared in package.json and installed via npm.
#
# Runtime requirements:
# ─────────────────────────────────────────────────────────────
# Node.js  >= 20.x
# npm      >= 10.x
#
# Production dependencies (see package.json):
# ─────────────────────────────────────────────────────────────
# express  ^4.18.2   - HTTP server and routing framework
# uuid     ^9.0.0    - RFC 4122 UUID generation for task IDs
#
# Install all dependencies:
#   npm install
#
# Install production dependencies only (mirrors Docker build):
#   npm install --omit=dev
# ─────────────────────────────────────────────────────────────
